FILE(GLOB BUILD_UIS "${CMAKE_CURRENT_SOURCE_DIR}/*.ui")

QT5_WRAP_UI(BUILD_UIS_H ${BUILD_UIS})

SET(BUILD_UIH_DIR ${CMAKE_CURRENT_BINARY_DIR} PARENT_SCOPE)

ADD_CUSTOM_TARGET(ui ALL DEPENDS ${BUILD_UIS_H})

MESSAGE(STATUS "Ui wrapped and added")
